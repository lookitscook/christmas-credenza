{
  "name": "christmas-credenza-exact",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Original tight-lights scene extracted from its live ChatGPT app iframe.",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && npm run build:standalone",
    "build:standalone": "node scripts/build-standalone.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "@rolldown/binding-wasm32-wasi": "1.2.8",
    "vite": "8.3.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
